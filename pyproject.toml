[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.ruff]
target-version = "py312"
line-length = 88
extend-exclude = [
  "release",
  "runtime",
  "work",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
